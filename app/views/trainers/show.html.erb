<h3>Trainer</h3>
<p>Name: <%= @trainer.name if @trainer.name %></p>
<p>Email: <%= @trainer.email if @trainer.email %></p>

<% if @trainer.id == current_trainer.id %>
	<p> <%= button_to "Create Pokemon", new_path(id: p), :class => "button medium", :method => :patch %> </p>
	<% end %>

<h3>Pokemon</h3>
<% Pokemon.all.where({trainer_id: @trainer.id}).each do |p| %>
	<p> <%= p.name %>, Level <%= p.level %>, <%= p.health %> Health 
	<%= button_to "Damage!", damage_path(id: p), :class => "button medium", :method => :patch %> </p>
<% end %>
